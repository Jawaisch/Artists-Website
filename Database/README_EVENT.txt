Bitte beachten das ein Neustart der Datenbank das Event deaktivert. Somit muss bei jedem Neustart dieses wieder manuell eingeschaltet werden.

Zudem befindet sich in der Tabelle Bild ein weiteres Attribut mit dem Namen Resv_Zeitstempel (Datentyp = datetime) //dient für den Warenkorb